# If we’re already inside the FHS shell, don’t let direnv reload the project
if [ -n "${SG200X_FHS:-}" ]; then
  # no-op inside FHS
  : 
else
  # Outside FHS: load the nix dev env (which will exec into FHS per your shell.nix)
  use nix
  # or: use flake
fi
